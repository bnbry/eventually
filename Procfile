alpha:    bundle exec rake run["alpha"]
beta:     bundle exec rake run["beta"]
charlie:  bundle exec rake run["charlie"]
delta:    bundle exec rake run["delta"]
overlord: bundle exec rake run["overlord"]
web:      open eventually.html && bash
